#include <stdio.h>
int main(){
    FILE* ptr = fopen("LOG.txt","w");   //w is denoting write a file here i.e., create a file named LOG.txt

    // //w checks if the file name is present and if not present it created the file
    
    return 0;
}