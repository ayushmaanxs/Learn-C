#include <stdio.h>

int main(){
    float p,r,t,si; //4 variables got created
    p = 100; //Principal
    r = 4; //Rate
    t = 2; //Time
    si = (p * r * t)/100;
    printf("SI of the given data is : %f",si);
    return 0;
}