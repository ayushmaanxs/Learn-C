#include <stdio.h>
#include <math.h>
int main()
{
    // int a;
    // printf("Enter a number: ");
    // scanf("%d",&a);
    // int root = sqrt(a);
    // printf("The square root is : %d",root);

    int q = pow(4, 3);
    printf("The value when 4 is raised by power 3 is: %d", q);
    return 0;
}